import BaseRegisters, { type BaseRegistersProps } from './BaseRegisters';

export default class Inputs extends BaseRegisters {
    constructor(props: BaseRegistersProps) {
        super(props, 'inputs');
    }
}
